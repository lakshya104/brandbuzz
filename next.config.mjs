/** @type {import('next').NextConfig} */
const nextConfig = {
    // webpack: (config) => {
    //     config.module.rules.push({
    //       test: /\.html$/,
    //       loader: 'null-loader',
    //     });
    //     return config;
    //   },
};

export default nextConfig;
